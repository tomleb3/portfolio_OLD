name: CD

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@main
        
      - name: Deploy 🚀
        uses: facebook/create-react-app@v5
        with:
            branch: gh-pages
            folder: build
            repositorty-name: tomleb3/portfolio
